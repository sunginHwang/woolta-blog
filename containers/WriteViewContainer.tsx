import React from 'react';

interface WriteViewContainerProps{
}

const WriteViewContainer: React.FC<WriteViewContainerProps> = ({}) => {
 return (
  <div>
   
  </div>
 );
};

export default WriteViewContainer;